import React from "react";
import AuthComponent from "@/components/AuthComponent";

const SignIn = () => {
  return <AuthComponent title={"Sign In"} desc={"sign up"} rout={"/Sign-Up"} />;
};

export default SignIn;
